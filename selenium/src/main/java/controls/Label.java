package controls;

import base.BaseElement;
import org.openqa.selenium.WebDriver;

public class Label extends BaseElement {

    public Label(WebDriver driver, String selector) {
        super(driver, selector);
    }
}
